{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal management-window access; control API credentials remain native-only.",
  "windows": ["main"],
  "permissions": ["core:window:allow-close", "dialog:allow-open"]
}
