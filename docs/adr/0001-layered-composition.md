# Compose CommonKit through ordered policy layers

CommonKit composes a public base, organization policy, personal kit, project loadout, and target overrides in that order. Organization security policy is a non-overridable floor: later layers may tighten its controls but cannot weaken them, preserving organizational guarantees while allowing increasingly specific customization.
