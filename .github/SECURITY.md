# Security Policy

Report vulnerabilities privately to security@unsold.group.

Do not include secrets in issues, discussions, logs, or screenshots. CommonKit excludes credentials and runtime identity from portable state. `mcp-local-relay` keeps credentials out of LaunchAgent plists and client MCP configuration; secrets should be provided through private environment files or a local secret manager.
